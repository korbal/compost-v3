{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "npm run generate && cp sitemap.xml dist/ && git add . && git commit -m \"deploy: added error page  \" && git push && push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^6.1.3",
    "nuxt": "3.0.0",
    "push-dir": "^0.4.1"
  },
  "dependencies": {
    "@gtm-support/vue-gtm": "^2.0.0",
    "vue-gtag": "^2.0.1"
  }
}